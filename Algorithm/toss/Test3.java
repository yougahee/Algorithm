/*
package Algorithm.toss;

import java.util.HashMap;

public class Test3 {

    public static void main(String[] args) {

        //String input = br.readLine();
        String[] input_split = input.split(" ");
        int[] int_input = new int[input_split.length];

        HashMap<Integer, Integer> hashMap = new HashMap<>();

        for(int i=0; i<input_split.length; i++) {
            int_input[i] = Integer.parseInt(input_split[i]);

            if(!hashMap.containsKey(int_input[i])) {
               // hashMap.put(int_input[i], Function.compute(int_input[i]));
            }
        }

        for(int i=0; i< int_input.length; i++) {
            System.out.print( hashMap.get(int_input[i]) + " ");
        }
    }
}
*/
